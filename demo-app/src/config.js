// Mutable runtime config — flipped by POST /admin/deploy. The "deploy" in this
// demo is a config flip; see the spec's incident fiction.
export const config = {
  version: "1.3.2",
  poolSize: 50,
};
